<h1>Explore our Flats</h1>

<% @flats.each do |flat| %>
  <%= link_to flat_path(flat['id']), target: '_blank' do %>
    <div style='background: lightblue; padding: 8px; margin: 16px'>
      <h2><%= flat['name'] %></h2>
      <p>💰<%= flat['price']  %> <%= flat['priceCurrency'] %></p>
    </div>
  <% end %>
<% end %>
